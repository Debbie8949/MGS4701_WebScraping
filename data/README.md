# Data

This folder contains processed datasets generated from the Nowcoder HAR data collection workflow.

Raw browser HAR files are retained locally and are not published here because they may contain session-related metadata.
